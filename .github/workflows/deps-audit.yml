name: Dependency audit
on:
  push:
    branches: [chore/deps-internal-utils]
  workflow_dispatch:
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Audit
        run: |
          npm install --no-save internal-utils@1.2.4 2>/dev/null || true
